@model IEnumerable<WebApplication6.Models.Reaction>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Blog)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReactionType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Blog.Body)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReactionType.ReactionName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ReactionID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ReactionID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ReactionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
